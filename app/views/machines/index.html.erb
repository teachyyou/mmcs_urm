<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Homepage</title>
</head>
<body>
<div style="display: flex; height: 100vh; width: 100%;
 flex-direction:column; align-content: stretch; overflow: ;">
  <div style="background-color: #007bff; color: white; 
  padding: 10px; text-align: center; margin:0px;">URM Machines</div>
  <div style="display: flex; flex-direction:row">
    <div style="width: 50%; border-right: 1px solid #ccc; ">
      
      <div style = "display:flex; flex-direction:column; align-content:center; 
      justify-content:flex-start; height:100vh; margin-top: 20px;
      overflow-y: auto"> <%# todo %>
        
        <% @machines.each do |machine| %>
          <button style="margin-top: 10px; margin-bottom:10px;padding: 20px; 
          border: 3px solid  #007bff; background-color:rgb(243, 243, 243);"
          class="show-machine-button"
          data-machine-id="<%= machine.id %>"
          onmouseover="this.style.backgroundColor='rgb(158, 201, 246)'" 
          onmouseout="this.style.backgroundColor='rgb(243, 243, 243)'"
          onmousedown="this.style.backgroundColor='rgb(138, 187, 240)'"
          onmouseup="this.style.backgroundColor='rgb(158, 201, 246)'" 
          <%# todo  add functionality%>
          >
          
          <%= machine.name %>
          
          </button>
        <% end %>
      </div>
    </div>
    <div style="display: flex; flex-direction: column; width: 50%; padding-left: 20px; 
    margin-top: auto; margin-bottom: auto; min-height: 100vh;">
      <div style = "height: 70%; border-bottom: 1px solid #ccc;"> <%# todo %>
        <h1 id="selected-machine-name"></h1>
        <p id="selected-machine-description" style = "background-color: #f3f3f3; min-height: 20vh;"></p>
        <button>Запустить</button>
        <%= button_to "Редактировать", '/add_new_machine', method: :get, class: 'home-buttons'%>
      </div>    
      <div style = "height: 30%;"> <%# todo %>    
        <div style="margin-top: 20px;">
          <%= button_to "Создать", '/add_new_machine', method: :get%>
          
          <button>Раскодировать</button>
          
        </div>
      </div>
    </div>
  </div>
  
</div>


<script>
    document.addEventListener('DOMContentLoaded', function() {
      document.querySelectorAll('.show-machine-button').forEach(button => {
        button.addEventListener('click', function() {
            const machineId = this.getAttribute('data-machine-id');
            const machineName = document.getElementById('selected-machine-name');
            const machineDescription = document.getElementById('selected-machine-description');
            fetch('<%= show_machine_path(id: 0) %>'.replace('0', machineId), {
                method: 'GET',
                headers: {
                    'Content-Type': 'application/json',
                }
            })
            .then(response => response.json())
            .then(data => {
                machineName.textContent = data.name;
                machineDescription.textContent = data.description;
            })
            .catch(error => {
                console.error('Error:', error);
            });
        });
      });
    });
</script>

</body>
</html>